# Scala stays on the 3.3.x LTS line. The pin covers the whole group because each platform
# publishes its own artifact, such as scala3-library_sjs1.
updates.pin = [
  { groupId = "org.scala-lang", version = "3.3." },
  { groupId = "org.scala-sbt", artifactId = "sbt", version = "1." }
]
